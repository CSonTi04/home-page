Place your background image here as `background.jpg`.

It is excluded from version control by `.gitignore` to avoid committing large binary files.
To include it in the repo, remove the `assets/background.jpg` line from `.gitignore`.
